#ifndef HASH_RECON_H
#define HASH_RECON_H

#include <string>

// Déclaration de la fonction analyzeHash
// Elle prend une chaîne de caractères (le hachage) et affiche une suggestion.
void analyzeHash(const std::string& hash);

#endif // HASH_RECON_H
